/*
	System Manager
	
	This will automatically manage a domain's status, and will email registrants as their domain is about to expire
	as a reminder to re-confirm their domain registration (unless the TLD manager has specified their TLD does not expire).
	
	TODO!! TODO!!
	This is just a standard template program at the moment. To be worked on.
*/
#include <stdio.h>
#include <sqlite3.h>

int main()
{
	return 0;
}